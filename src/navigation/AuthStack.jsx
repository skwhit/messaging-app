import { View, Text } from "react-native";
import { LoginScreen } from "../screens";

const AuthStack = () => {
  return (
    <View>
      <LoginScreen />
    </View>
  );
};

export default AuthStack;
